// To tell our accounts system that we want users 
// to log-in via a username, we simple add and
// Accounts.ui config block in /clients/helpers.
Accounts.ui.config({
    passwordSignupFields: 'USERNAME_ONLY'
});